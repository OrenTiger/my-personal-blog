@using MyPersonalBlog.Models;
@model IEnumerable<Tag>

<h4 class="text-info">Теги</h4>

@foreach (var item in Model)
{
    <span>@Html.ActionLink(@item.Name, "", "Search", new { tag = item.Id }, new { @class = "label label-success" })</span>
}